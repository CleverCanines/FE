import { ThemedText } from "@/components/ThemedText"
import { ThemedView } from "@/components/ThemedView"

export default function taskModal() {
    return (
        <ThemedView>
            <ThemedText>Task Modal</ThemedText>
        </ThemedView>
    );
}